[build-system]
requires = ["scikit-build-core>=0.5.0", "pybind11", "packaging>=23.0"]
build-backend = "scikit_build_core.build"

[project]
name = "tinyserve"
version = "0.1.0"
requires-python = ">=3.10"

[tool.scikit-build]
cmake.verbose = true
logging.level = "INFO"